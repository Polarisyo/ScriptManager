server:
  port: 8123
  servlet:
    context-path: /api
spring:
  application:
    name: ScriptManager
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/script_manager
    username: root
    password: 618920
knife4j:
  enable: true
  openapi:
    title: 接口文档
    version: 1.0
    group:
      default:
        api-rule: package
        api-rule-resources:
          - backend.controller

